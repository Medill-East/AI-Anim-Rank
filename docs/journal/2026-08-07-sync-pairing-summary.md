# AnimeRank 决策摘要：跨设备同步

日期：2026-08-07（Asia/Shanghai）

## 当前状态

- 排行榜主站已部署在 `https://ai-anim-rank.play-with-experiences.workers.dev`。
- 原有二维码只迁移 `phrase + salt` 到另一台设备；客户端没有实例化 `SyncClient`，也没有调用同步 Worker，因此实际只能依赖 IndexedDB 或 JSON 备份。
- `ai-anim-rank-sync` Worker 与 APAC D1 已创建并部署，主站 origin 的 CORS 预检已返回 204。

## 决策与理由

- 保留二维码作为“迁移恢复凭证”，不把进度数据放进二维码。
- 保险库接入后：首次自动拉取并合并远端密文；本地标记保存后异步上传；提供“立即同步”和明确的同步状态。
- 无同步端点时，界面必须明确说明二维码只能迁移凭证，不能同步标记，避免造成错误安全感。
- 同步数据继续端到端加密，Cloudflare 只接收密文；冲突沿用条目级最后修改时间合并。

## 本次改动与验证

- 增加 HTTP `SyncTransport`，支持 Worker 的 GET/PUT、ETag 版本和 409 冲突。
- `RankingWorkspace` 接入保险库和同步队列；同步结果回写本地进度库。
- `SyncSettings` 增加同步状态、立即同步按钮和准确的二维码说明。
- 已补齐 D1、迁移和同步 Worker，并使用固定主站 origin 配置 CORS。
- 已通过：TypeScript、同步核心测试、32 个同步/排行榜 DOM 测试；重新发布前再次通过 97 个核心测试、27 个 DOM 测试、lint 与 diff 检查。
- 发布冒烟检查发现 Vinext/Vite 不会用 `process.env.VITE_SYNC_BASE_URL` 注入浏览器端配置；已改为 `import.meta.env.VITE_SYNC_BASE_URL`，并补充 `vite-env.d.ts` 类型声明，待重新发布确认线上状态文案。
- 真实端点预检后发现 Worker 旧协议只接受三字段密文，而客户端载荷还需要 `vaultId` 与协议 `version` 才能在另一台设备解密；已统一 Worker 的读写协议为五字段，并新增路径绑定校验。
- Worker 修正已部署（版本 `4123e352-0435-478f-b157-a5b307070f56`）；真实端点验证通过：CORS 预检 204、首次写入 201、读取完整五字段载荷、过期版本 409，临时测试数据已清理。
- 真实客户端使用临时保险库直接调用线上 Worker 返回 `synced`；同步失败提示更可能来自旧页面缓存或早期部署。同步入口已改为“创建私密保险库 / 连接已有保险库”，默认显示可复制连接密钥，二维码降为可选备用方式。
- 浏览器真实状态仍显示失败后，端到端复现为：设备 A 首次写入 `synced`，设备 B 拉取同一保险库 `unsynced`。Node 与 Cloudflare 的响应检查显示 ETag 被规范化为弱格式 `W/"1"`，客户端原先只接受强格式 `"1"`；客户端版本解析和 Worker `If-Match` 解析已兼容两种格式。
- 修正后的真实跨设备测试通过：设备 A `synced` 写入 1 条记录，设备 B `synced` 拉取 1 条记录并保留 `watched` / `recommended` 状态。

## 本次数据刷新（2026-08-07）

- 先按现有流程尝试抓取 AniList/Jikan；Jikan 深分页连续返回 504，捕获指针未切换，旧快照保持不变。
- 为完成一轮可发布刷新，重新抓取 AniList Top 600，并对 310 个已审核 MAL 映射读取 Jikan 逐作品详情；其中 127 条 Jikan 详情在短重试后从对应的 MyAnimeList 公开页面补取同一评分字段，未降低资格门槛。
- 候选报告包含 599 条作品，306 条通过三源评分、投票数和身份校验；正式发布 Top 300，快照版本更新为 `2026-08-07`。
- 新旧快照 300 个稳定 `workId` 全部保持一致，无新增或移除；99 条排名变化，最大移动 18 位，前十名未变；最高综合分 89.7，仍没有 90 分以上条目。
- 数据校验通过：普通快照校验与 release 校验均为 300 条，唯一作品 ID/排名完整；全量测试、类型检查与 lint 已通过。主站已重新部署，线上 Worker 版本为 `531c015c-f1f6-49a3-952a-17a762775a61`。

## 事故教训

- “二维码导入成功”只代表本地恢复凭证有效，不代表远端同步已经建立；必须同时验证客户端调用、端点配置和 Worker/D1 部署状态。
- `VITE_SYNC_BASE_URL` 过去只用于显示配置状态，文档措辞曾暗示同步能力，需让 UI 状态和真实链路一致。
- 端到端协议必须同时验证客户端请求、Worker 入参校验和 Worker 返回值；仅有模拟 transport 测试不足以证明线上接口可用。
- 密文本身不包含解密密钥；产品文案必须把“连接密钥”和“云端密文”分开命名，避免用户误以为拿到密文即可解码。
- HTTP 版本头不能假设强 ETag；Cloudflare 等边缘平台可能返回弱 ETag，客户端和条件写入端都要兼容 `W/"n"`。

## 未决问题与下一步

- 主站已重新发布并确认线上页面显示同步已启用；下一步只需在两台真实设备使用同一恢复凭证确认浏览器端拉取/上传体验。
- 观察首次同步失败时的提示，以及冲突合并后的结果。

完整会话证据（当前运行时可取得的已清洗部分）：[2026-08-07-sync-pairing-transcript-partial.md](./2026-08-07-sync-pairing-transcript-partial.md)
